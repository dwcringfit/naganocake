<div class = "row">
	<div class="col-xs-4">
		<%= attachment_image_tag(@item, :image, format: 'jpeg', size: '300x300', fallback: "no_image.jpg") %>
	</div>
	<div class="col-xs-8">
		 商品名  <%= @item.name %><br>
	     商品説明 <%= @item.context %><br>
         税込価格（税抜価格）     <%= @item.price_tax_included %>円 （ <%= @item.price %>円 ）<br>
	     販売ステータス  <%= @item.is_sale ? "販売中" : "売り切れ" %><br>
	     <%= link_to "編集する",edit_admin_item_path(@item), class: "btn btn-success edit_admin_item_#{@item.id}" %><br>
	</div>
</div>