<table class="table table-hover table-bordered table-inverse">
		<thead>
			<th><h4>ジャンル検索</h4></th>
		</thead>
			<tbody>
				<td><%= link_to "全て表示",items_path, method: :get %></td>
					<% genres.each do |genre| %>
				<tr>
					<td><%= link_to genre.name, items_path(genre_id: genre.id), method: :get %></td>
				</tr>
					<% end %>
		</tbody>
</table>